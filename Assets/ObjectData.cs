using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectData : MonoBehaviour
{
    public float minValue = 0f;  // Objeye özel minimum değer
    public float maxValue = 100f;  // Objeye özel maksimum değer
}
